const initialState = {
  activePostsComments: [],
  loading: false
}

export default function commentsReducer (state = initialState, actions) {
  switch(actions.type) {
    default:
      return state
  }
}
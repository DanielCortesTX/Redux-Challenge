const initialState = {
  allCategories: [],
  activeCategory: {},
  loading: false
}

export default function categoriesReducer (state=initialState, action) {
  switch(action.type){
    default:
      return state
  }
}